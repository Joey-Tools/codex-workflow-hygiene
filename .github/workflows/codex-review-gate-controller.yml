name: Codex Review Gate Controller

on:
  issue_comment:
    types: [created, edited]
  workflow_dispatch:
    inputs:
      operation:
        description: Operation to run for one pull request
        required: true
        type: choice
        options:
          - reconcile
          - begin-review
        default: reconcile
      pr_number:
        description: Pull request number
        required: true
        type: number
      expected_head_sha:
        description: Exact current 40-character pull request head SHA
        required: true
        type: string
      request_comment_id:
        description: Optional GitHub comment ID used only as an evidence hint
        required: false
        type: string
      request_review:
        description: Post a fresh Codex request during begin-review; ignored during reconcile
        required: false
        type: boolean
        default: true

permissions:
  actions: write
  checks: read
  contents: read
  issues: write
  pull-requests: read

concurrency:
  group: codex-review-gate-controller-${{ github.repository }}-${{ github.event.issue.number || inputs.pr_number }}
  cancel-in-progress: false

jobs:
  codex-review-gate-controller:
    name: codex/review-gate-controller
    if: >-
      ${{
        (
          github.event_name == 'workflow_dispatch' &&
          github.ref_type == 'branch' &&
          github.ref_name == github.event.repository.default_branch
        ) ||
        (
          github.event_name == 'issue_comment' &&
          (github.event.action == 'created' || github.event.action == 'edited') &&
          github.event.issue.pull_request &&
          github.event.sender.login == 'chatgpt-codex-connector[bot]' &&
          github.event.sender.type == 'Bot' &&
          github.event.comment.user.login == 'chatgpt-codex-connector[bot]' &&
          github.event.comment.user.type == 'Bot'
        )
      }}
    runs-on: ${{ vars.CODEX_REVIEW_GATE_USE_UBUNTU_LATEST == 'true' && 'ubuntu-latest' || 'ubuntu-slim' }}
    timeout-minutes: 14
    steps:
      - name: Refresh Codex review gate
        id: controller
        uses: JoeyTeng/codex-review-gate-action@v2
        env:
          CODEX_REVIEW_GATE_REQUEST_AUTHOR_PERMISSION: ${{ vars.CODEX_REVIEW_GATE_REQUEST_AUTHOR_PERMISSION == 'any' && 'any' || 'write' }}
        with:
          github_token: ${{ github.token }}
          pr_number: ${{ github.event_name == 'workflow_dispatch' && inputs.pr_number || github.event.issue.number }}
          expected_head_sha: ${{ github.event_name == 'workflow_dispatch' && inputs.expected_head_sha || '' }}
          operation: ${{ github.event_name == 'workflow_dispatch' && inputs.operation || 'reconcile' }}
          request_comment_id: ${{ github.event_name == 'workflow_dispatch' && inputs.request_comment_id || github.event.comment.id }}
          request_review: ${{ github.event_name == 'workflow_dispatch' && inputs.request_review || false }}
          limits_profile: ${{ vars.CODEX_REVIEW_GATE_LIMITS_PROFILE == 'expanded' && 'expanded' || 'default' }}
