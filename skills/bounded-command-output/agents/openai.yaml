interface:
  display_name: "Bounded Command Output"
  short_description: "Keep high-output commands scoped and compact."
  default_prompt: "Use $bounded-command-output to keep this command scoped, pollable, and compact."
policy:
  allow_implicit_invocation: true
