interface:
  display_name: "Bounded Command Output"
  short_description: "Bound long-running or high-output commands."
  default_prompt: "Use $bounded-command-output to keep this command scoped, time-bounded, pollable, and compact."
policy:
  allow_implicit_invocation: true
