interface:
  display_name: "Codex Rules Hygiene"
  short_description: "Audit Codex rules without changing files."
  default_prompt: "Use $codex-rules-hygiene to run a read-only audit of my Codex rules and propose exact apply steps without changing files."
